# Enable AndroidX and Jetifier for dependency migration/compat
android.useAndroidX=true
android.enableJetifier=true

# Enable Gradle Build Cache for faster incremental builds in CI/local
org.gradle.caching=true